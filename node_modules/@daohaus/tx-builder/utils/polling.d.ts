import { IFindQueryResult } from '@daohaus/data-fetch-utils';
type PollFetch = (...args: any) => Promise<any>;
type PollTest = (result?: any) => boolean;
type Poll = ({ poll, test, interval, variables, maxTries, }: {
    poll: PollFetch;
    test: PollTest;
    interval?: number;
    variables: Parameters<typeof poll>;
    onPollStart?: () => void;
    onPollSuccess?: (result: any) => void;
    onPollError?: (error: unknown) => void;
    onPollTimeout?: (error: unknown) => void;
    maxTries?: number;
}) => void;
export declare const pollLastTX: PollFetch;
export declare const testLastTX: (result: IFindQueryResult<any> | undefined) => boolean;
export declare const standardGraphPoll: Poll;
export {};
