import { ProcessedContract, ContractLego, ABI, ArbitraryState } from '@daohaus/utils';
import { Keychain, ValidNetwork } from '@daohaus/keychain-utils';
export declare const processContractLego: ({ contract, chainId, localABIs, appState, rpcs, explorerKeys, }: {
    contract: ContractLego;
    chainId: ValidNetwork;
    localABIs: Record<string, ABI>;
    appState: ArbitraryState;
    rpcs: Keychain;
    explorerKeys: Keychain;
}) => Promise<ProcessedContract>;
