import { Keychain, ValidNetwork } from '@daohaus/keychain-utils';
import { ActionError, DeepDecodedAction } from './decoding';
export declare const deepDecodeProposalActions: ({ chainId, actionData, rpcs, explorerKeys, }: {
    chainId: ValidNetwork;
    actionData: string;
    rpcs?: Keychain | undefined;
    explorerKeys?: Keychain | undefined;
}) => Promise<(DeepDecodedAction | ActionError)[]>;
