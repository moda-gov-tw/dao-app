import { ABI, ArbitraryState } from '@daohaus/utils';
import { ValidNetwork } from '@daohaus/keychain-utils';
export declare const getABIstore: () => Promise<ArbitraryState>;
export declare const getCachedABI: ({ address, chainId, }: {
    address: string;
    chainId: ValidNetwork;
}) => Promise<ABI | undefined>;
export declare const cacheABI: ({ address, chainId, abi, }: {
    address: string;
    chainId: ValidNetwork;
    abi: ABI;
}) => Promise<boolean>;
