import { FieldValues } from 'react-hook-form';
import { TXLifeCycleFns } from '@daohaus/tx-builder';
import { FormLego } from '@daohaus/form-builder-base';
import { LookupType } from '@daohaus/utils';
type BuilderProps = {
    form: FormLego;
    defaultValues?: FieldValues;
    customFields?: LookupType;
    onSubmit?: (formValues: FieldValues) => void | Promise<(formValues: FieldValues) => void>;
    lifeCycleFns?: TXLifeCycleFns;
    targetNetwork?: string;
    submitButtonText?: string;
};
export declare enum StatusMsg {
    Compile = "Compiling Transaction Data",
    Request = "Requesting Signature",
    Await = "Transaction Submitted",
    TxErr = "Transaction Error",
    TxSuccess = "Transaction Success",
    PollStart = "Syncing TX (Subgraph)",
    PollSuccess = "Success: TX Confirmed!",
    PollError = "Sync Error (Subgraph)",
    NoContext = "Missing TXBuilder Context"
}
export declare const FormBuilder: ({ form, defaultValues, customFields, submitButtonText, targetNetwork, lifeCycleFns, onSubmit, }: BuilderProps) => JSX.Element;
export {};
