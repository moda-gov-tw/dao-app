import { Buildable, FormSegment } from '@daohaus/ui';
import { FieldLegoBase, LookupType } from '@daohaus/utils';
import { ComponentProps } from 'react';
type Props = Omit<Buildable<ComponentProps<typeof FormSegment> & {
    fields: FieldLegoBase<LookupType>[];
}>, 'formArea'>;
export declare const SegmentRender: (props: Props) => JSX.Element;
export {};
