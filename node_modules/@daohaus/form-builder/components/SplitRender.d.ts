import { Buildable } from '@daohaus/ui';
import { FieldLego } from '../types';
type SplitColumnProps = {
    id: string;
    rows: {
        rowId: string;
        left: FieldLego;
        right: FieldLego;
    }[];
};
export declare const SplitColumnLayout: ({ rows, ...props }: Buildable<SplitColumnProps>) => JSX.Element;
export {};
