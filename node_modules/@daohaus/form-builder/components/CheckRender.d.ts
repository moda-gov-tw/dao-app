import { ComponentProps } from 'react';
import { Buildable, CheckGate } from '@daohaus/ui';
type CheckRenderProps = Omit<Buildable<ComponentProps<typeof CheckGate> & {
    gateLabel: string;
    components: any[];
}>, 'fields'>;
export declare const CheckRender: ({ gateLabel, ...props }: Buildable<CheckRenderProps>) => JSX.Element;
export default CheckRender;
