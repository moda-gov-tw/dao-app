/// <reference types="react" />
type SegmentType = {
    actionButton?: React.ReactNode;
    collapsible?: boolean;
    defaultOpen?: boolean;
    title?: string | React.ReactNode;
    description?: string | React.ReactNode;
    formArea: React.ReactNode;
    infoArea?: React.ReactNode;
    error?: React.ReactNode;
};
export declare const CollapsibleFormSegment: ({ actionButton, defaultOpen, title, description, formArea, infoArea, error, }: SegmentType) => JSX.Element;
export {};
