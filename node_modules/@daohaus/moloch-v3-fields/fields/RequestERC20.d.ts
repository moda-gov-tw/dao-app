import { Buildable } from '@daohaus/ui';
export declare enum InputStates {
    Loading = 0,
    InvalidNetwork = "Invalid Network",
    CorruptTokenData = "Corrupt Token Data"
}
export declare const RequestERC20: (props: Buildable<{
    amtId?: string;
    addressId?: string;
    safeAddressId?: string;
}>) => JSX.Element;
