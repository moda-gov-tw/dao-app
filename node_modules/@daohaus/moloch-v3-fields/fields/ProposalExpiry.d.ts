import { Buildable, Field } from '@daohaus/ui';
type ProposalExpiryProps = Buildable<Field & {
    defaultValue?: string;
    label: string;
}>;
export declare const ProposalExpiry: ({ id, defaultValue, rules, ...props }: ProposalExpiryProps) => JSX.Element;
export {};
