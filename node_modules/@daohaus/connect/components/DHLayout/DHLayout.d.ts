import { ComponentProps, ReactNode } from 'react';
import { AppSwitcher } from '@daohaus/ui';
import { DaoHausNavProps } from '../DaoHausNav/DaoHausNav.types';
export declare const DHLayout: ({ navLinks, dropdownLinks, dropdownTriggerLabel, children, appNavLinks, leftNav, pathname, footer, }: DaoHausNavProps & {
    children: ReactNode;
    leftNav?: ReactNode;
    appNavLinks?: import("@daohaus/ui").AppSwitcherProps | undefined;
    pathname: string;
    footer?: ReactNode;
}) => JSX.Element;
