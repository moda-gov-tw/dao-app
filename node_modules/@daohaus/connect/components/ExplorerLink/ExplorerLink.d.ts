import React from 'react';
import { ValidNetwork } from '@daohaus/keychain-utils';
type ExplorerLinkProps = {
    children?: React.ReactNode;
    chainId?: ValidNetwork;
    address?: string;
    className?: string;
    type?: string;
};
export declare const ExplorerLink: ({ children, chainId, address, className, type, }: ExplorerLinkProps) => JSX.Element;
export {};
