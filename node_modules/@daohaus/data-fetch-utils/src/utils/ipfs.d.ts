export declare const PINATA_PIN_JSON = "https://api.pinata.cloud/pinning/pinJSONToIPFS";
type JSONResponse = {
    IpfsHash: string;
    pinSize: number;
    Timestamp: string;
    isDuplicate: boolean;
};
export declare const pinataPostJSON: ({ creds, jsonString, }: {
    creds: {
        pinata_api_key: string;
        pinata_api_secret: string;
    };
    jsonString: string;
}) => Promise<JSONResponse>;
export {};
