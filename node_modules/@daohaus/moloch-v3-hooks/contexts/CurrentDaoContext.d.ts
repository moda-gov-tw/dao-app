import { ValidNetwork } from '@daohaus/keychain-utils';
import React, { ReactNode } from 'react';
type CurrentDaoContextType = {
    daoId?: string;
    daoChain?: ValidNetwork;
    proposalId?: string;
    memberAddress?: string;
    userAddress?: string;
    updateFilter: (filterKey: string, filter: any) => void;
    updateOrder: (filterKey: string, filter: any) => void;
    getOrder: (filterKey: string) => any;
    getFilter: (filterKey: string) => any;
};
export declare const CurrentDaoContext: React.Context<CurrentDaoContextType>;
type TargetDao = {
    daoId?: string;
    daoChain?: ValidNetwork;
    proposalId?: string;
    memberAddress?: string;
};
type CurrentContextProps = {
    children: ReactNode;
    targetDao: TargetDao;
    userAddress?: string;
};
export declare const CurrentDaoProvider: ({ children, targetDao, userAddress, }: CurrentContextProps) => JSX.Element;
export declare const useCurrentDao: () => CurrentDaoContextType;
export {};
