import { IFindQueryResult, IListQueryArguments, IListQueryResults } from '@daohaus/data-fetch-utils';
import { Keychain, ValidNetwork } from '@daohaus/keychain-utils';
import { FindRecordQueryRes, ListRecordQueryRes, Record_Filter, Record_OrderBy } from './types';
export declare const findRecord: ({ networkId, recordId, graphApiKeys, }: {
    networkId: ValidNetwork;
    recordId: string;
    graphApiKeys?: Keychain | undefined;
}) => Promise<IFindQueryResult<FindRecordQueryRes>>;
export declare const listRecords: ({ networkId, filter, ordering, paging, graphApiKeys, }: IListQueryArguments<Record_OrderBy, Record_Filter>) => Promise<IListQueryResults<Record_OrderBy, Record_Filter, ListRecordQueryRes['records']>>;
