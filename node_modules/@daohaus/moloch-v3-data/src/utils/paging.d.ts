import { Paging } from '@daohaus/data-fetch-utils';
export declare const DEFAULT_RECORDS_PER_PAGE = 25;
export interface ILightEntity {
    readonly id: string;
}
export declare const paginateResponse: <T>(items: T[], paging: Paging) => T[];
export declare const createPaging: <T extends ILightEntity>(items: T[], paging: Paging) => {
    pageItems: T[];
    nextPaging: {
        pageSize: number;
        offset: number | undefined;
        lastId: string | undefined;
    } | undefined;
    previousPaging: {
        pageSize: number;
        offset: number | undefined;
    } | undefined;
};
