import { ValidNetwork } from '@daohaus/keychain-utils';
import { MetaTransaction } from '@daohaus/utils';
export declare const estimateFunctionalGas: ({ chainId, constractAddress, from, value, data, }: {
    chainId: ValidNetwork;
    constractAddress: string;
    from: string;
    value: string;
    data: string;
}) => Promise<number | undefined>;
export declare const gasEstimateFromActions: ({ actions, chainId, safeId, }: {
    actions: MetaTransaction[];
    chainId: ValidNetwork;
    safeId: string;
}) => Promise<number | undefined>;
