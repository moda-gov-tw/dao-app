"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getContractAbi = exports.getContractAddressesForChain = void 0;
const abis_1 = require("@daohaus/abis");
const keychain_utils_1 = require("@daohaus/keychain-utils");
/**
 * Get addresses of contracts that have been deployed to the
 * Ethereum mainnet or a supported testnet. Throws if there are
 * no known contracts deployed on the corresponding chain.
 * @param contractKey The desired Contract
 * @param chainId The desired chainId
 */
const getContractAddressesForChain = (contractKey, chainId) => {
    var _a;
    if (!((_a = keychain_utils_1.CONTRACT_KEYCHAINS === null || keychain_utils_1.CONTRACT_KEYCHAINS === void 0 ? void 0 : keychain_utils_1.CONTRACT_KEYCHAINS[contractKey]) === null || _a === void 0 ? void 0 : _a[chainId])) {
        return null;
    }
    return keychain_utils_1.CONTRACT_KEYCHAINS[contractKey][chainId];
};
exports.getContractAddressesForChain = getContractAddressesForChain;
/**
 * Get abi of contracts that have been added to
 * the local-abi package
 * @param abiKey The desired Contract
 */
const getContractAbi = (abiKey) => {
    if (!abis_1.LOCAL_ABI[abiKey]) {
        return null;
    }
    return abis_1.LOCAL_ABI[abiKey];
};
exports.getContractAbi = getContractAbi;
//# sourceMappingURL=contract-meta.js.map