import { Dispatch, SetStateAction } from 'react';
import { NetworkConfigs } from '@daohaus/keychain-utils';
import { UserProfile, ConnectLifecycleFns } from './types';
export declare const numberToHex: (number: number) => string;
export declare const truncateAddress: (addr: string) => string;
export declare const loadProfile: ({ address, setProfile, setProfileLoading, shouldUpdate, lifeCycleFns, networks, }: {
    address: string;
    setProfile: Dispatch<SetStateAction<UserProfile>>;
    setProfileLoading: Dispatch<SetStateAction<boolean>>;
    shouldUpdate: boolean;
    lifeCycleFns?: ConnectLifecycleFns | undefined;
    networks: NetworkConfigs;
}) => Promise<void>;
