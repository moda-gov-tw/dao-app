import { ValidNetwork } from '@daohaus/keychain-utils';
export declare const generateGnosisUiLink: ({ chainId, address, }: {
    chainId: ValidNetwork;
    address?: string | undefined;
}) => string;
