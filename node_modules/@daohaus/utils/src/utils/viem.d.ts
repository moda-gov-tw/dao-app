import { ValidNetwork, Keychain } from '@daohaus/keychain-utils';
import { HttpTransport, PublicClient } from 'viem';
export declare const createTransport: ({ chainId, rpcs, retryCount, }: {
    chainId: ValidNetwork;
    rpcs: Keychain;
    retryCount?: number | undefined;
}) => HttpTransport;
export declare const createViemClient: ({ chainId, rpcs, }: {
    chainId: ValidNetwork;
    rpcs?: Keychain | undefined;
}) => PublicClient;
