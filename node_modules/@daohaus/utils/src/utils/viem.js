"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createViemClient = exports.createTransport = void 0;
const keychain_utils_1 = require("@daohaus/keychain-utils");
const viem_1 = require("viem");
const createTransport = ({ chainId, rpcs = keychain_utils_1.HAUS_RPC, retryCount = 10, }) => {
    const rpc = rpcs[chainId];
    if (!rpc)
        return (0, viem_1.http)();
    return (0, viem_1.http)(rpc, { retryCount });
};
exports.createTransport = createTransport;
const createViemClient = ({ chainId, rpcs = keychain_utils_1.HAUS_RPC, }) => {
    const transport = (0, exports.createTransport)({
        chainId,
        rpcs,
    });
    return (0, viem_1.createPublicClient)({
        chain: keychain_utils_1.VIEM_CHAINS[chainId],
        transport,
    });
};
exports.createViemClient = createViemClient;
//# sourceMappingURL=viem.js.map