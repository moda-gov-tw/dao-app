"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.isAdmin = exports.isGovernor = exports.isManager = void 0;
const isManager = (permissionLevel) => {
    return (Number(permissionLevel) === 2 ||
        Number(permissionLevel) === 3 ||
        Number(permissionLevel) === 6 ||
        Number(permissionLevel) === 7);
};
exports.isManager = isManager;
const isGovernor = (permissionLevel) => {
    return (Number(permissionLevel) === 4 ||
        Number(permissionLevel) === 5 ||
        Number(permissionLevel) === 6 ||
        Number(permissionLevel) === 7);
};
exports.isGovernor = isGovernor;
const isAdmin = (permissionLevel) => {
    return (Number(permissionLevel) === 1 ||
        Number(permissionLevel) === 3 ||
        Number(permissionLevel) === 5 ||
        Number(permissionLevel) === 7);
};
exports.isAdmin = isAdmin;
//# sourceMappingURL=shamanPermission.js.map