import { AccountProfile } from '@daohaus/utils';
export declare const getProfileForAddress: ({ address, rpcUri, }: {
    address: string;
    rpcUri?: string | undefined;
}) => Promise<AccountProfile>;
