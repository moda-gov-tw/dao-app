import { AccountProfile } from '@daohaus/utils';
import { ENSDomain } from '../types';
export declare const transformProfile: ({ address, ensDomain, }: {
    address: string;
    ensDomain?: ENSDomain;
}) => AccountProfile;
