import { Buildable, Field } from '../../../types/formAndField';
export declare const WrappedTextArea: ({ id, rules, helperText, long, full, icon, success, warning, error, number, address, className, placeholder, defaultValue, value, disabled, hidden, rightAddon, label, loading, info, }: Buildable<Field>) => JSX.Element;
