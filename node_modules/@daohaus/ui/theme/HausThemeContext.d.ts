import { ReactNode } from 'react';
import { DefaultTheme } from 'styled-components';
import { ReactSetter } from '@daohaus/utils';
import './global/fonts.css';
import { ToastProps } from '../components/molecules/Toast';
import { ThemeOverrides } from '../types/theming';
type HausUI = {
    theme: DefaultTheme;
    setTheme: ReactSetter<DefaultTheme>;
    toggleLightDark: () => void;
    setToast: (toast: ToastProps) => void;
};
type ProviderProps = {
    children: ReactNode;
    defaultDark?: DefaultTheme;
    defaultLight?: DefaultTheme;
    startDark?: boolean;
    themeOverrides?: ThemeOverrides;
};
export declare const HausThemeContext: import("react").Context<HausUI>;
export declare const HausThemeProvider: ({ children, defaultDark, defaultLight, startDark, themeOverrides, }: ProviderProps) => JSX.Element;
export default HausThemeProvider;
