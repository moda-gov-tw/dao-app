import { MolochV3Dao, MolochV3Proposal } from '@daohaus/moloch-v3-data';
import { DecodedMultiTX } from '@daohaus/tx-builder';
export type ProposalDataPoint = {
    displayType: 'data' | 'member';
    label: string;
    value: string;
};
export declare const formatAddtionalDataPoints: (proposal: MolochV3Proposal, actionData: DecodedMultiTX, dao: MolochV3Dao, daoChain: string) => Promise<ProposalDataPoint[] | undefined>;
