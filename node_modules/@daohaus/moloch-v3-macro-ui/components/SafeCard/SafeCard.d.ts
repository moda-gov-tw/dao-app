import { DaoSafe, MolochV3Dao } from '@daohaus/moloch-v3-data';
type SafeCardProps = {
    dao: MolochV3Dao;
    safe: DaoSafe;
    daoChain: string;
    includeLinks?: boolean;
};
export declare const SafeCard: ({ dao, safe, daoChain, includeLinks, }: SafeCardProps) => JSX.Element;
export {};
