type SafeActionMenuProps = {
    ragequittable: boolean;
    safeAddress: string;
    daoChain: string;
    daoId: string;
};
export declare const SafeActionMenu: ({ ragequittable, safeAddress, daoChain, daoId, }: SafeActionMenuProps) => JSX.Element | null;
export {};
