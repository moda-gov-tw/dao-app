import { ProposalActionConfig } from './ProposalActionData';
type ProposalWarningProps = {
    proposalType?: string;
    decodeError?: boolean;
    txHash: string;
    proposalActionConfig?: ProposalActionConfig;
    daoChain: string;
};
export declare const ProposalWarning: ({ proposalType, decodeError, txHash, proposalActionConfig, daoChain, }: ProposalWarningProps) => JSX.Element | null;
export {};
