import { MolochV3Proposal } from '@daohaus/moloch-v3-data';
import { DeepDecodedMultiTX as DecodedMultiTX } from '@daohaus/tx-builder';
import { MulticallAction } from '@daohaus/utils';
export type ProposalActionConfig = {
    sensitiveProposalTypes?: Record<string, boolean>;
    actionToProposalType?: Record<string, string>;
    proposalTypeWarning?: Record<string, string>;
};
type ProposalActionDataProps = {
    daoChain: string;
    daoId: string;
    proposal: MolochV3Proposal;
    proposalActionConfig?: ProposalActionConfig;
    actionData?: DecodedMultiTX | null;
    actionsMeta?: MulticallAction[];
    decodeError: boolean;
};
export declare const ProposalActionData: ({ daoChain, daoId, proposal, proposalActionConfig, actionData, decodeError, }: ProposalActionDataProps) => JSX.Element;
export {};
