import { ComponentProps } from 'react';
import { Button } from '@daohaus/ui';
type ProfileLinkProps = {
    href?: string;
    to: string;
    selected?: boolean;
    disabled?: boolean;
    linkType?: 'internal' | 'external' | 'no-icon-external';
    hideIcon?: boolean;
    target?: string;
    rel?: string;
} & Partial<ComponentProps<typeof Button>>;
export declare const ButtonRouterLink: ({ to, target, disabled, children, linkType, hideIcon, rel, ...buttonProps }: ProfileLinkProps) => JSX.Element;
export {};
