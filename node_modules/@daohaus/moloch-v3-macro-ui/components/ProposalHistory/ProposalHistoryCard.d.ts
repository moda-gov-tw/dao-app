import { MolochV3Proposal } from '@daohaus/moloch-v3-data';
import { ProposalHistoryElement } from '../../utils';
type ProposalHistoryCardProps = {
    element: ProposalHistoryElement;
    proposal: MolochV3Proposal;
    daoChain: string;
    daoId: string;
    includeLinks?: boolean;
};
export declare const ProposalHistoryCard: ({ element, proposal, daoChain, daoId, includeLinks, }: ProposalHistoryCardProps) => JSX.Element;
export {};
