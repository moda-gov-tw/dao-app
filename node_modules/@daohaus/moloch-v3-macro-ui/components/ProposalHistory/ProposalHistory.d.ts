import { Keychain } from '@daohaus/keychain-utils';
type ProposalHistoryProps = {
    proposalId: string;
    daoChain: string;
    daoId: string;
    includeLinks?: boolean;
    graphApiKeys?: Keychain;
};
export declare const ProposalHistory: ({ proposalId, daoChain, daoId, includeLinks, graphApiKeys, }: ProposalHistoryProps) => JSX.Element | null;
export {};
