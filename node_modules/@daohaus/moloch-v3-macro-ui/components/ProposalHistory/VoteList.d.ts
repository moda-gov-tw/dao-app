import { MolochV3Proposal } from '@daohaus/moloch-v3-data';
type VoteListProps = {
    proposal: MolochV3Proposal;
    votes: MolochV3Proposal['votes'];
    daoChain: string;
    daoId: string;
    includeLinks?: boolean;
};
export declare const VoteList: ({ votes, proposal, daoChain, daoId, includeLinks, }: VoteListProps) => JSX.Element;
export {};
