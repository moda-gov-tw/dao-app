import { MolochV3Proposal } from '@daohaus/moloch-v3-data';
import { ProposalActionConfig } from '../ProposalActionData';
import { TXLego } from '@daohaus/utils';
type ProposalDetailsContainerProps = {
    daoChain: string;
    daoId: string;
    proposal: MolochV3Proposal;
    includeLinks: boolean;
    proposalActionConfig?: ProposalActionConfig;
    txLegos?: Record<string, TXLego>;
};
export declare const ProposalDetailsContainer: ({ daoChain, daoId, proposal, includeLinks, proposalActionConfig, txLegos, }: ProposalDetailsContainerProps) => JSX.Element;
export {};
