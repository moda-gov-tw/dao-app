import { ProposalDataPoint } from '../../utils/proposalDetailsHelpers';
type MemberDataPointProps = {
    daoChain: string;
    daoId: string;
    dataPoint: ProposalDataPoint;
    includeLinks: boolean;
};
export declare const MemberDataPoint: ({ dataPoint, includeLinks, daoChain, daoId, }: MemberDataPointProps) => JSX.Element;
export {};
