import { MolochV3Proposal } from '@daohaus/moloch-v3-data';
import { DecodedMultiTX } from '@daohaus/tx-builder';
import { ProposalActionConfig } from '../ProposalActionData';
type ProposalDetailsProps = {
    daoChain: string;
    daoId: string;
    proposal: MolochV3Proposal;
    includeLinks: boolean;
    actionData?: DecodedMultiTX | null;
    decodeError: boolean;
    proposalActionConfig?: ProposalActionConfig;
};
export declare const ProposalDetails: ({ daoChain, daoId, proposal, includeLinks, actionData, decodeError, proposalActionConfig, }: ProposalDetailsProps) => JSX.Element;
export {};
