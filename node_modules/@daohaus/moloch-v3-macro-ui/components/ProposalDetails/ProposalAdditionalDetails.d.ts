import { MolochV3Proposal } from '@daohaus/moloch-v3-data';
import { DecodedMultiTX } from '@daohaus/tx-builder';
type ProposalAddtionalDetailsProps = {
    daoChain: string;
    daoId: string;
    proposal: MolochV3Proposal;
    includeLinks: boolean;
    actionData?: DecodedMultiTX | null;
};
export declare const ProposalAddtionalDetails: ({ daoChain, daoId, proposal, includeLinks, actionData, }: ProposalAddtionalDetailsProps) => JSX.Element | null;
export {};
