import { Keychain, ValidNetwork } from '@daohaus/keychain-utils';
type SafesProps = {
    daoChain: ValidNetwork;
    daoId: string;
    includeLinks?: boolean;
    graphApiKeys?: Keychain;
};
export declare const SafesList: ({ daoChain, daoId, includeLinks, graphApiKeys, }: SafesProps) => JSX.Element | null;
export {};
