import { ValidNetwork } from '@daohaus/keychain-utils';
type MemberProfileMenuProps = {
    daoChain: ValidNetwork;
    daoId: string;
    memberAddress: string;
    allowLinks?: boolean;
    allowMemberMenu?: boolean;
};
export declare const MemberProfileMenu: ({ daoChain, daoId, memberAddress, allowLinks, allowMemberMenu, }: MemberProfileMenuProps) => JSX.Element | null;
export {};
