import { ValidNetwork } from '@daohaus/keychain-utils';
import { MolochV3Member } from '@daohaus/moloch-v3-data';
type MemberProfileCardProps = {
    daoChain: ValidNetwork;
    daoId: string;
    member: MolochV3Member;
    allowLinks?: boolean;
    allowMemberMenu?: boolean;
};
export declare const MemberProfileCard: ({ daoChain, daoId, member, allowLinks, allowMemberMenu, }: MemberProfileCardProps) => JSX.Element;
export {};
