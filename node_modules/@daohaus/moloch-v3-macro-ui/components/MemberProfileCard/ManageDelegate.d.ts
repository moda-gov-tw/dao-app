import { ValidNetwork } from '@daohaus/keychain-utils';
type ManageDelegateProps = {
    daoChain: ValidNetwork;
    daoId: string;
    defaultMember?: string;
};
export declare const ManageDelegate: ({ daoChain, daoId, defaultMember, }: ManageDelegateProps) => JSX.Element;
export {};
