import { MouseEvent } from 'react';
import { Column } from 'react-table';
export type DaoTableProps<T extends object> = {
    tableData: T[];
    columns: Column<T>[];
    hasNextPaging?: boolean;
    handleLoadMore?: (event: MouseEvent<HTMLButtonElement>) => void;
    handleColumnSort?: (orderBy: string, orderDirection: 'asc' | 'desc') => void;
    sortableColumns: string[];
};
export declare function DaoTable<T extends object>({ tableData, columns, hasNextPaging, handleLoadMore, handleColumnSort, sortableColumns, }: DaoTableProps<T>): JSX.Element;
