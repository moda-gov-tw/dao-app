import { Field } from '@daohaus/ui';
import { Noun } from '@daohaus/utils';
type SearchInputProps = {
    searchTerm: string;
    setSearchTerm: (term: string) => void;
    totalItems: number;
    noun?: Noun;
} & Partial<Field>;
export declare const SearchInput: ({ searchTerm, setSearchTerm, totalItems, noun, ...inputProps }: SearchInputProps) => JSX.Element;
export {};
