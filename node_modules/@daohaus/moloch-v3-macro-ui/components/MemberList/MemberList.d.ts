import { ValidNetwork } from '@daohaus/keychain-utils';
type MemberListProps = {
    daoChain: ValidNetwork;
    daoId: string;
    allowLinks?: boolean;
    allowMemberMenu?: boolean;
};
export declare const MemberList: ({ daoChain, daoId, allowLinks, allowMemberMenu, }: MemberListProps) => JSX.Element;
export {};
